import { array, object, string } from 'zod'
import { TaskSchema } from '../models/project.model'


/* export const createProjectSchema = object({
    body: object({
        name: string({required_error: 'Name is required'}),
        tasks: typeof TaskSchema,
    })
}) */